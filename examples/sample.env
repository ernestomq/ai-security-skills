# =============================================================
# HARDCODED VALUES — extracted by hardcode-sentinel v2.0
# Generated: 2026-04-19T10:32:00Z
# Source files scanned: hooks/post-tool.sh, .claude/settings.json, scripts/deploy.sh
# Git history checked: yes — 50 commits checked
# =============================================================
# USAGE:
#   1. Fill in REPLACE_WITH_REAL_VALUE with your actual values
#   2. NEVER commit this file — it is in .gitignore
#   3. Load before running: source .security/hardcoded-values.env
#   4. Or use a secrets manager (Doppler, 1Password, Vault)
# =============================================================

# --- CRITICAL ---

# hooks/post-tool.sh:14 — OpenAI API key — used in Authorization header
# found value: sk-***[last6: ef456]
# rotate at: https://platform.openai.com/api-keys
OPENAI_API_KEY=REPLACE_WITH_REAL_VALUE

# .claude/settings.json:8 — MCP server env block — Slack integration
# found value: https://hooks.slack.com/services/***[last6: abc123]
# rotate at: https://api.slack.com/apps → Incoming Webhooks
SLACK_WEBHOOK_URL=REPLACE_WITH_REAL_VALUE

# scripts/deploy.sh:22 — Database connection string — production DB
# found value: postgres://admin:***@prod-db.internal:5432/myapp
# rotate at: your database provider admin panel
DATABASE_URL=REPLACE_WITH_REAL_VALUE

# --- HIGH ---

# scripts/deploy.sh:5 — machine-specific path — not portable
# found value: /home/ernesto/projects/myapp
DEPLOY_PATH=REPLACE_WITH_REAL_VALUE

# .claude/settings.json:31 — MCP server hostname — internal infrastructure
# found value: myserver.internal
INTERNAL_HOST=REPLACE_WITH_REAL_VALUE

# --- MEDIUM ---

# scripts/deploy.sh:18 — external API URL — should be configurable
# found value: https://api.myservice.com/v2
API_BASE_URL=REPLACE_WITH_REAL_VALUE

# --- GIT HISTORY (rotation mandatory) ---

# Found in commit a1b2c3d on 2026-03-15 in scripts/old-deploy.sh:8
# Removed in commit d4e5f6g but still accessible in git history
# found value: ghp_***[last6: xyz789]
# rotate at: https://github.com/settings/tokens
GITHUB_TOKEN=ROTATE_IMMEDIATELY
